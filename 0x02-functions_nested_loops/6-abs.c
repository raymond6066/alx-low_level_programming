#include "main.h"

/**
 * _abs - computes the absolute value of a number
 * @n: number to check
 *
 * Description: prints absolute value of integer i
 * Return:0 or 1
 */
int _abs(int n)
{
return (n >= 0 ? n : n * -1);
}
